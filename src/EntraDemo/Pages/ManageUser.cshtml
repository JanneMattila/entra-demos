@page
@model ManageUserModel
@{
    ViewData["Title"] = "Entra demo";
}

@using (Html.BeginForm())
{
<div class="container">
    <div class="row">
        <div class="col-sm">
@if (!string.IsNullOrEmpty(Model.NewUserPassword))
{
    <h3>New user successfully created!</h3>
    <div class="form-group">
        @Html.LabelFor(m => m.NewUserPassword, "New user password")
        @Html.TextBoxFor(m => m.NewUserPassword, new { @class = "form-control" })
    </div>

    <div>
        <a href="~/" class="btn btn-secondary" role="button">Close</a>
    </div>
}
else
{
            @Html.HiddenFor(m => m.UserProperties.ID)

            @if (Model.IsNewUser)
            {
                <h3>Add new user</h3>
            }
            else
            {
                <h3>Edit user</h3>
                <div class="form-group">
                    @Html.LabelFor(m => m.UserProperties.DisplayName, "Display Name")
                    @Html.TextBoxFor(m => m.UserProperties.DisplayName, new { @class = "form-control" })
                </div>
            }

            <div class="form-group">
                @Html.LabelFor(m => m.UserProperties.FirstName, "First Name")
                @Html.TextBoxFor(m => m.UserProperties.FirstName, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.UserProperties.LastName, "Last Name")
                @Html.TextBoxFor(m => m.UserProperties.LastName, new { @class = "form-control" })
            </div>
            @if (Model.IsNewUser)
            {
                <div class="form-group">
                    @Html.LabelFor(m => m.UserProperties.UserPrincipalName, "UserProperties Principal Name")
                    @Html.TextBoxFor(m => m.UserProperties.UserPrincipalName, new { @class = "form-control" })
                </div>
            }
            <div class="form-group">
                @Html.LabelFor(m => m.UserProperties.JobTitle, "Job Title")
                @Html.TextBoxFor(m => m.UserProperties.JobTitle, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.UserProperties.Department)
                @Html.TextBoxFor(m => m.UserProperties.Department, new { @class = "form-control" })
            </div>
            <div class="form-check">
                @Html.LabelFor(m => m.UserProperties.AccountEnabled, "Account Enabled", new { @class = "form-check-label" })
                @Html.CheckBoxFor(m => m.UserProperties.AccountEnabled, new { @class = "form-check-input" })
            </div>

            <div class="mb-3">
                @if (Model.IsNewUser)
                {
                    <button type="submit" class="btn btn-primary" name="action" value="save">Add user</button>
                }
                else
                {
                    <button type="submit" class="btn btn-primary" name="action" value="save">Save</button>
                }
                <a href="~/" class="btn btn-secondary" role="button">Cancel</a>
            </div>

            @if (!Model.IsNewUser)
            {
                <div class="mb-3">
                    <br />
                    <hr />
                    <br />
                            <button type="submit" class="btn btn-danger" name="action" value="remove" onclick="return confirm('Are you sure you want to remove user?')">Remove user</button>
                </div>
            }
}
        </div>
    </div>
</div>
}
